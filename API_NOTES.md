# SPEC.md — 大学講義 音声ノートAIアプリ 仕様書

> **バージョン:** 1.0  
> **作成日:** 2026-05-17  
> **ステータス:** フェーズ1（仕様確定）

---

## 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| アプリ名 | 大学講義 音声ノートAI |
| 目的 | 講義音声をアップロードし、文字起こし・要約・要点を自動生成する |
| 対象ユーザー | 大学生・研究者・講義を録音する学習者 |
| 公開方法 | GitHub Pages（静的ホスティング） |
| 実装言語 | HTML / CSS / JavaScript（フロントエンドのみ・サーバー不要） |

---

## 2. 技術スタック

```
フロントエンド:  HTML5 / CSS3 / Vanilla JavaScript (ES Modules)
AIモデル:        Gemini API（Google）
APIキー管理:     localStorage（ブラウザ保存）
デプロイ:        GitHub Pages
外部依存:        なし（CDN・フレームワーク不使用で依存関係ゼロ）
```

> ⚠️ **モデル名に関する重要注意点**  
> ご要件に `gemini-1.5-flash` と記載がありましたが、2026年5月時点の公式ドキュメントでは  
> **`gemini-2.5-flash`（またはプレビュー版 `gemini-3-flash-preview`）** が最新推奨モデルです。  
> 本仕様書では安定版として **`gemini-2.5-flash`** を採用します（API_NOTES.md 参照）。

---

## 3. 機能要件

### 3.1 必須機能（MVP）

| No. | 機能 | 説明 |
|-----|------|------|
| F-01 | APIキー設定 | localStorageに保存・読込・削除。コードへの直書き禁止 |
| F-02 | 音声ファイルアップロード | ファイル選択UI。ドラッグ＆ドロップ対応 |
| F-03 | ファイルバリデーション | 形式・サイズのチェックとエラー表示 |
| F-04 | 文字起こし | 音声をテキストに変換して表示 |
| F-05 | 3行要約 | 講義内容を3文以内で要約 |
| F-06 | 箇条書き要点 | 重要ポイントを5〜10項目で抽出 |
| F-07 | 処理中表示 | ローディングインジケーター（API呼び出し中） |
| F-08 | エラーハンドリング | APIエラー・ネットワークエラーの日本語表示 |

### 3.2 追加機能（フェーズ2以降）

- [ ] 結果のコピーボタン（クリップボード）
- [ ] Markdown形式でダウンロード
- [ ] 複数ファイルのバッチ処理
- [ ] 処理履歴の保存（localStorage）
- [ ] 言語選択（日本語・英語・自動検出）

---

## 4. 画面・UI仕様

### 4.1 画面構成（シングルページ）

```
┌────────────────────────────────────────┐
│  🎓 大学講義 音声ノートAI              │  ← ヘッダー
├────────────────────────────────────────┤
│  ⚙️ APIキー設定エリア                  │  ← 折りたたみ可能
│  [●●●●●●●●●●●●] [保存] [削除]         │
├────────────────────────────────────────┤
│                                        │
│  ┌──────────────────────────────────┐  │
│  │  音声ファイルをここにドロップ     │  │  ← アップロードゾーン
│  │  または [ファイルを選択]          │  │
│  └──────────────────────────────────┘  │
│  対応形式: MP3, WAV, AAC, FLAC, OGG   │
│  上限: 20MB（インライン）/ それ以上はFiles API │
│                                        │
│  [🎙️ 解析開始]                         │  ← メインボタン
├────────────────────────────────────────┤
│  📝 文字起こし結果                      │  ← 結果エリア（解析後表示）
│  ─────────────────────────────────    │
│  💡 3行要約                            │
│  ─────────────────────────────────    │
│  📌 重要ポイント                        │
└────────────────────────────────────────┘
```

### 4.2 デザイン方針

- カラー：ダークネイビー × アクセントブルー（学術的・落ち着いた雰囲気）
- フォント：システムフォント（Noto Sans JP fallback）
- レスポンシブ：モバイル対応（最小幅 375px）
- アクセシビリティ：キーボード操作・aria属性対応

---

## 5. ファイル構成

```
/
├── index.html       # メインHTML（UI全体）
├── style.css        # スタイルシート
├── app.js           # メインロジック（APIキー管理・UI制御）
├── gemini.js        # Gemini API呼び出し専用モジュール
├── README.md        # 使い方説明（GitHub表示用）
└── SPEC.md          # 本ファイル
```

---

## 6. セキュリティ要件

| 要件 | 対策 |
|------|------|
| APIキー漏洩防止 | コードへの直書き禁止。localStorageのみで管理 |
| CORSリスク | Gemini APIはブラウザから直接呼び出し可能（CORS対応済み） |
| ファイル検証 | MIMEタイプ・拡張子・サイズをクライアント側でチェック |
| 通信 | HTTPS必須（GitHub Pagesはデフォルトで対応） |

> ⚠️ **重要:** localStorageに保存したAPIキーはブラウザの開発者ツールから参照可能です。  
> 共有PC・公共端末では使用後に「削除」ボタンでキーを消去してください。

---

## 7. 制約・前提条件

- バックエンドサーバーは持たない（純粋なフロントエンドアプリ）
- ユーザーは自身のGemini APIキーを用意する必要がある
- インターネット接続必須（Gemini API呼び出しのため）
- 対応ブラウザ：Chrome / Firefox / Safari / Edge（最新版）

---

## 8. フェーズロードマップ

| フェーズ | 内容 | 状態 |
|---------|------|------|
| Phase 1 | 仕様書作成（SPEC.md / API_NOTES.md） | ✅ 完了 |
| Phase 2 | index.html + CSS の実装 | 🔜 次フェーズ |
| Phase 3 | app.js + gemini.js の実装 | 🔜 次フェーズ |
| Phase 4 | GitHub Pages デプロイ・動作確認 | 🔜 次フェーズ |
| Phase 5 | 追加機能・UI改善 | 🔜 将来 |
